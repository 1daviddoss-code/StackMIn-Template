# Templates Directory

Upload your background images and overlay templates here.

- `public/templates/backgrounds/` - For 16:9 background images
- `public/templates/overlays/` - For transparent PNG overlays

Once uploaded, you can reference them in `src/data/templates.ts` using paths like `/templates/backgrounds/my-bg.jpg`.
