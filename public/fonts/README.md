# Custom Font

Upload your custom `.ttf` font file here and name it exactly `custom-font.ttf`.

The application is pre-configured to use `/fonts/custom-font.ttf` as the primary font for the entire dashboard and all generated slides.
